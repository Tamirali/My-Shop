// ऑर्डर को सीधे WhatsApp पर भेजने का फंक्शन
function addToCart(productId) {
    const product = state.products.find(p => p.id === productId);
    
    if (product) {
        const phoneNumber = "918797221991"; // आपका नंबर
        const message = `नमस्ते तामीर अली जी, मुझे आपकी दुकान से यह सामान खरीदना है:
📦 *प्रोडक्ट:* ${product.name}
💰 *कीमत:* ₹${product.price}
🆔 *ID:* ${product.id}

कृपया इसकी डिलीवरी की जानकारी दें।`;

        // WhatsApp का असली लिंक बनाना
        const whatsappUrl = `https://wa.me/${phoneNumber}?text=${encodeURIComponent(message)}`;
        
        // WhatsApp खोलना
        window.open(whatsappUrl, '_blank');
    } else {
        alert("प्रोडक्ट की जानकारी नहीं मिली!");
    }
}
